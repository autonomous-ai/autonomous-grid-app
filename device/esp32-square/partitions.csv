# Partition table for the 16MB flash on the Waveshare ESP32-S3-Touch-LCD-4B.
#
# (The reference firmware's copy of this file names the ESP32-S3-Touch-AMOLED-1.75C in this header — a
# copy-paste from the round sibling it was forked from. The layout is right, the board name was not.)
#
# Dual-OTA: two app slots + otadata. Nothing in this firmware writes them yet, but the layout is here
# from the start on purpose — grid-app carries the .bin and is meant to reflash the panel over the same
# USB cable it already talks on (docs/overview.md, step 5). Changing the table later means a manual
# esptool flash of every device in existence, which is exactly the thing self-update exists to avoid.
#
# ~7.9MB per slot against a current image well under half that, so the headroom is not the constraint.
# Name,   Type, SubType, Offset,   Size
nvs,      data, nvs,     0x9000,   0x6000
otadata,  data, ota,     0xf000,   0x2000
phy_init, data, phy,     0x11000,  0x1000
ota_0,    app,  ota_0,   0x20000,  0x7E0000
ota_1,    app,  ota_1,   0x800000, 0x7E0000
